{
  "name": "wnba-api",
  "version": "1.0.0",
  "description": "A REST API for WNBA play-by-play and schedule data using Express and SQLite",
  "main": "main.js",
  "scripts": {
    "start": "node main.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^6.0.0"  // Use a compatible version
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT"  // Specify a license if applicable
}
